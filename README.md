# h# Simple YouTube Clone


Static single-page app that shows a gallery of YouTube videos and plays them in a modal. Designed for GitHub Pages.


## How to publish on GitHub Pages


1. Create a new repository on GitHub (e.g. `simple-youtube-clone`).
2. Clone it locally and copy the project files into the folder.


```bash
git init
git add .
git commit -m "Initial commit: simple youtube clone"
git branch -M main
git remote add origin https://github.com/<your-username>/<repo>.git
git push -u origin mainigh-vision
